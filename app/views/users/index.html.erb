<div class='banner-blank-strip'></div>
<br>
<table   style="margin: auto;"><tr><td><td>
	<td>

		  <h3>Users</h3>
		</div>
		<table class="table table-striped">
		  <thead>
		    <tr>
		      <th>ID</th>
		      <th>Email</th>
		      <th>Comp Lists</th>
		      <th>Actions</th>
		    </tr>
		  </thead>
		  <tbody>
		    <% @users.each do |user| %>
		      <tr>
		        <td><%= user.id %></td>
		        <td><%= link_to user.email, user_path(user) %></td>
		        <td><%= user.myprops.group_by(&:list_num).count %></td>
		        <td>
		          <%= link_to 'Edit', edit_user_path(user), :class => 'btn btn-mini' %>
		          <%= link_to 'Destroy', user_path(user), :method => :delete, :confirm => 'Are you sure?', :class => 'btn btn-mini btn-danger' %>
		        </td>
		      </tr>
		    <% end %>
		  </tbody>
		</table>

		<%= link_to 'New', new_user_path, :class => 'btn' %>
		
<hr>		<%= link_to "Properties", properties_path, :class => 'btn' %>
			<%= link_to 'Listed Properties', list_all_myprops_path, :class => 'btn' %>
			<%= link_to 'Owners', owners_path, :class => 'btn' %>
<hr>
			<%= link_to 'Towns', home_properties_path, :class => 'btn btn-info' %>
			<br>
		
	</td>
	<td></td></tr>
</table>
<br><br>
